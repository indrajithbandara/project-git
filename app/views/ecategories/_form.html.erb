<%= form_for(ecategory) do |f| %>
  <% if ecategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ecategory.errors.count, "error") %> prohibited this ecategory from being saved:</h2>

      <ul>
      <% ecategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
