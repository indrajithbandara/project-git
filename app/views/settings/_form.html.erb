<%= form_for(setting) do |f| %>
  <% if setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(setting.errors.count, "error") %> prohibited this setting from being saved:</h2>

      <ul>
      <% setting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title_website %>
    <%= f.text_field :title_website %>
  </div>

  <div class="field">
    <%= f.label :title_travel %>
    <%= f.text_field :title_travel %>
  </div>

  <div class="field">
    <%= f.label :title_event %>
    <%= f.text_field :title_event %>
  </div>

  <div class="field">
    <%= f.label :title_footer %>
    <%= f.text_field :title_footer %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
